{{#each model as |item|}}
  <section class="row color-group {{item.color}}-bricks">
    <h2>{{item.color}}</h2>
    {{#each item.bricks as |brick|}}
      {{lego-brick brick=brick onSale=item.onSale}}
    {{/each}}
  </section>
{{/each}}

{{outlet}}
